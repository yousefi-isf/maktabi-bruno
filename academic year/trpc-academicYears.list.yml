info:
  name: /trpc/academicYears.list
  type: http
  seq: 1

http:
  method: GET
  url: "{{BACK_URL}}/trpc/academicYears.list"
  headers:
    - name: Origin
      value: "{{FRONT_URL}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
