info:
  name: /trpc/auth.switchSchool
  type: http
  seq: 4

http:
  method: POST
  url: "{{BACK_URL}}/trpc/auth.switchSchool"
  headers:
    - name: Origin
      value: "{{FRONT_URL}}"
  body:
    type: json
    data: |-
      {
        "schoolId" : "b49fc8a6-c664-4568-ab4b-7ec15803fa11"  
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
