info:
  name: /trpc/auth.me
  type: http
  seq: 3

http:
  method: GET
  url: "{{BACK_URL}}/trpc/auth.me"
  headers:
    - name: Origin
      value: "{{FRONT_URL}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
